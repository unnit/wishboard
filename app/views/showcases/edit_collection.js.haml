- if @collection.errors.blank?
  $(".edit-collection-#{@collection.id}").hide();
  $(".view-collection-#{@collection.id}").find(".coll-name").text("#{truncate(@collection.name, length: 20)}")
  $(".view-collection-#{@collection.id}").fadeIn();
- else
  $(".edit-collect-err").text("#{@collection.errors.full_messages.first}");
