= content_for :title do
  - title = "#{user.name} (@#{@profile.slug}) | Cocociti"
- if @profile.about.present?
  = content_for :meta_content do
    - meta_content = "#{@profile.about}"
.messages
.container-fluid.pattern-bg{style: "min-height: 50px;border-bottom: 1px solid #e5e5e5;line-height: 50px;", class: "prof-user-#{user.id}"}
  .container.padding0
    %ul.mbottom0.padding0
      %li.inline-block.mright20
        = link_to myprofile_path(@profile.slug) do
          %span.black-fg.collections-count= user.collections.count + 2
          %span.grey-fg Showcases
      %li.inline-block.mright20
        = link_to following_path(@profile.slug) do
          %span.black-fg.prof-following-count= user.following.count
          %span.grey-fg Following
      %li.inline-block.mright20
        = link_to followers_path(@profile.slug) do
          %span.black-fg.prof-followers-count= user.followers.count
          %span.grey-fg Followers
      %li.inline-block.mright20
        = link_to wiki_path(@profile.slug) do
          %span.grey-fg Wiki
      %li.inline-block.hidden-xs.pull-right
        - if current_user && current_user.same_user?(user)
          = link_to settings_path do
            .btn.grey-fg.prof-sett
              %span.glyphicon.glyphicon-cog.pull-left
      %li.inline-block.hidden-xs.pull-right
        - if current_user && current_user != user
          .text-center.padding0{class: "follow-set-#{user.id}"}
            = link_to user_toggle_follow_path(user), remote: true, method: :post do
              .btn.no-mar-pad.loader-out-follow-button.follow-button.btn-grey
                = submit_tag "#{ current_user.following?(user) ? 'Following' : "Follow" }", class: "loader-button btn loader-in-follow-button padding0 font12 grey-fg"
                .loader-effect.btn.loader-in-follow-button.grey-fg{style: "display:none;"}
                  .ball-clip-rotate{style: "height:22px;"}
                    %div ...
.container-fluid
  .container.padding0
    .col-xs-12.col-sm-12.text-center.padding0.mbottom10{style: "border-bottom: 1px solid #e5e5e5;"}
      = cl_image_tag user.avatar, class: "img-circle mob-mtop10", style: "z-index: 1;margin-top: -45px;", width: 100, height: 100, crop: :fill, gravity: :face
      .col-xs-12.col-sm-12.padding0.mbottom10.text-right.text-center-mob.font-bit-large.mob-mtop0{style: "margin-top: -52px;"}
        - if @profile.facebook.present?
          = link_to "https://facebook.com/#{@profile.facebook.split('/').last}", target: "_blank" do
            .fa.fa-facebook-official.grey-fg.mright5
        - if @profile.twitter.present?
          = link_to "https://twitter.com/#{@profile.twitter.split('/').last}", target: "_blank" do
            .fa.fa-twitter-square.grey-fg.mright5
        - if @profile.instagram.present?
          = link_to "https://instagram.com/#{@profile.instagram.split('/').last}", target: "_blank" do
            .fa.fa-instagram.grey-fg.mright5
        - if @profile.linkedin.present?
          = link_to "https://linkedin.com/in/#{@profile.linkedin.split('/').last}", target: "_blank" do
            .fa.fa-linkedin-square.grey-fg.mright5
        - if @profile.google_plus.present?
          = link_to "https://plus.google.com/#{@profile.google_plus.split('/').last}", target: "_blank" do
            .fa.fa-google-plus-square.grey-fg.mright5
        - if @profile.website.present?
          - if @profile.website.split('/').first == "https:" || @profile.website.split('/').first == "http:"
            = link_to @profile.website, target: "_blank" do
              .fa.fa-globe.grey-fg.mright5
          - elsif @profile.website.split(".").first == "www"
            = link_to "http://#{@profile.website}", target: "_blank" do
              .fa.fa-globe.grey-fg.mright5
          - else
            = link_to "http://www.#{@profile.website}", target: "_blank" do
              .fa.fa-globe.grey-fg.mright5
        - if @profile.other_url.present?
          - if @profile.other_url.split('/').first == "https:" || @profile.other_url.split('/').first == "http:"
            = link_to @profile.other_url, target: "_blank" do
              .fa.fa-link.grey-fg
          - elsif @profile.other_url.split(".").first == "www"
            = link_to "http://#{@profile.other_url}", target: "_blank" do
              .fa.fa-link.grey-fg
          - else
            = link_to "http://www.#{@profile.other_url}", target: "_blank" do
              .fa.fa-link.grey-fg
      %h4.font21.mtop0= (truncate user.name, length: 40)
      %h6.grey-fg
        - if user.location && user.location.name.present?
          .fa.fa-anchor
          = (truncate user.location.name, length: 40)
      %h5.grey-fg= (truncate @profile.about, length: 250)
      %ul.mbottom10.padding0.visible-xs
        %li.inline-block
          - if current_user && current_user != user
            .btn.text-center{class: "follow-set-#{user.id}"}
              = link_to user_toggle_follow_path(user), remote: true, method: :post do
                .btn.no-mar-pad.loader-out-follow-button.follow-button.btn-grey
                  = submit_tag "#{ current_user.following?(user) ? 'Following' : "Follow" }", class: "loader-button btn loader-in-follow-button padding0 font12 grey-fg"
                  .loader-effect.btn.loader-in-follow-button.grey-fg{style: "display:none;"}
                    .ball-clip-rotate{style: "height:22px;"}
                      %div ...
        %li.inline-block
          - if current_user && current_user.same_user?(user)
            = link_to settings_path do
              .btn.grey-fg.prof-sett
                %span.glyphicon.glyphicon-cog.pull-left
#cont-wrapper.modal.fade.padding0
